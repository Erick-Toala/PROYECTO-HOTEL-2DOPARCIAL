//creamos un models .ts (interface) para los parametros de los usuarios
export interface Usuarios{
    usuario: string;
    nombre: string;
    contrasena: string;
    correo: string;
    telefono: string;
}